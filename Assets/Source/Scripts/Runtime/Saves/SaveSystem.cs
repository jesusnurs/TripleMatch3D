using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveSystem : MonoBehaviour
{
    /*
    public static SaveSystem Instance { get; private set; }
        
    public static Action<SaveData> OnDataUpdated { get; set; }

    public SaveData GetSaveData()
    {
        return SaveFileManager.GetLocalSaveData();
    }

    public void UpdateSaveData(SaveData saveData)
    {
        SaveFileManager.UpdateSaveData(saveData);
    }

    public void Init()
    {
        Instance = this;
        SaveFileManager.OnSaveFileChanged += (data => { OnDataUpdated?.Invoke(data); });
    }
    */
}
